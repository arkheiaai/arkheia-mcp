# Arkheia MCP Trust Server + Enterprise Proxy -- top-level requirements

# MCP SDK (pinned to installed version)
mcp==1.26.0

# HTTP
httpx>=0.27.1

# FastAPI + server (proxy)
fastapi>=0.115.0
uvicorn[standard]>=0.31.1
starlette>=0.49.1  # CVE-2025-62727 fix (DoS via Range header); CVE-2025-54121 fix (multipart DoS)

# Pydantic
pydantic>=2.0.0
pydantic-settings>=2.0.0

# YAML
pyyaml>=6.0

# Async file I/O
aiofiles>=23.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Load testing (optional, Phase 2)
# locust>=2.20.0
