.venv/
__pycache__/
*.pyc
.env
*.log
audit.jsonl
registry_audit.jsonl   # registry auth-decision receipts (default path)
*.bak.yaml
.pytest_cache/
install_service.bat
start-enterprise-proxy.bat

# Mutation-campaign crash-safety sidecar (tools/mutate_f11_passthrough.py).
# Its presence means a run died holding a planted mutant; it must never be
# committed, and neither must the mutant it is there to undo.
*.mutation-original

# F10 sweep: local 3.12 (CI interpreter) venv, and the runtime-materialised
# origin/master baseline suite the mutation counterfactual uses.
.venv312/
proxy/tests/test_zz_preexisting_baseline_tmp.py
