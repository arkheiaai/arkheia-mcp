# Arkheia Enterprise Proxy -- Python dependencies
# No pinned versions for flexibility; CI pins via pip-compile.
# Minimum versions are tested working.

fastapi>=0.135.1
uvicorn[standard]>=0.31.1
httpx>=0.27.1
PyJWT>=2.13.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
pyyaml>=6.0
aiofiles>=23.0

# Loads .env before proxy.* imports (proxy/main.py) -- imported unconditionally
# at the entrypoint, but was ABSENT here (latent deploy crash on every startup).
python-dotenv>=1.2.2  # CVE-2026-28684 (symlink attack in set_key/unset_key) fix

# AES-256-GCM encrypt/decrypt for profiles (proxy/crypto/profile_crypto.py) --
# imported unconditionally but was ABSENT here (latent deploy crash importing
# encrypted profiles); CI previously installed it ad hoc so tests stayed green.
cryptography>=46.0.7  # CVE-2026-39892 (buffer overflow) + CVE-2026-26007 (EC key validation) fixes

# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27.1

# Load testing (Phase 2)
# locust>=2.20.0
starlette==1.3.1  # CVE-2024-47874, CVE-2025-54121, CVE-2025-62727

anyio>=4.14.2  # GHSA-82r6-8w77-94w6 (anyio IDNA-2008 TLS cert-validation) — transitive pin
